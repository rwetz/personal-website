// Loaded lazily by <LazyMotion> in App.jsx so the animation engine stays out of
// the first-load bundle. domAnimation covers everything this site uses:
// animate/exit, variants, hover/tap gestures, and whileInView.
import { domAnimation } from 'framer-motion'

export default domAnimation
